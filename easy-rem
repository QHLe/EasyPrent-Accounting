#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")"

if [ -x ".venv/bin/python" ]; then
  exec .venv/bin/python -m src.easyprent_accounting.cli "$@"
fi

exec python3 -m src.easyprent_accounting.cli "$@"
